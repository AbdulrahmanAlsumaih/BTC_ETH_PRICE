
function exchangeRecommendedBuy(a,b) {

    if (parseInt(a) > parseInt(b))
    {
        return "Blockchain"
    }
    else
    {
        return "Coinbase"
    }
}

export default exchangeRecommendedBuy;
